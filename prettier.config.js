module.exports = {
    // Specify the file types to format
    overrides: [
        {
            files: "*.js",
            // Set the indentation to 4 spaces
            options: {
                tabWidth: 4,
            },
        },
    ],
};
